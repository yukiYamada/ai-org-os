# L2: Product Core OS

この領域は、**このプロダクト自体のOS（規約・コア構造）**を配置する。

- 目的: 常に参照される一次ルールの固定
- 含めるもの:
  - 組織共通規約
  - チーム運用規約
  - 強制手段（Hookなど）
- 含めないもの:
  - 学習結果ログ（L3へ）
  - 再利用バンドル成果物（L1へ）

> 既存の `org/` `teams/*/rules.md` `.githooks/` は段階的にこの領域へ整理する。
