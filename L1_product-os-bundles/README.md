# L1: Product OS Bundles

この領域は、**このプロダクトで生成・提供される「学習済み開発集団OSのセット」**を配置する。

- 目的: 他プロジェクトへ持ち出せる「運用済みパッケージ」の保管
- 含めるもの:
  - 移植可能なチーム構成バンドル
  - 学習済みルールセットのスナップショット
- 含めないもの:
  - コア規約本文（L2へ）
  - 日々の運用ログ（L3へ）

> 現在は初期化のみ。実体の初回配置は `IA-3x` タスクで段階移行する。
